[project]
dynamic = ["version"]
name = "krita_comfyui"
requires-python = ">=3.10"

[tool.uv]
package = false

[tool.ruff]
target-version = "py311"
line-length = 100
preview = true
extend-exclude = [
    "krita_comfyui/websockets",
    "krita.pyi",
]

[tool.ruff.lint]
ignore = ["E741"]
